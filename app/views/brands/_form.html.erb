<%= form_with(model: brand) do |form| %>
  <% if brand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(brand.errors.count, "error") %> prohibited this brand from being saved:</h2>

      <ul>
        <% brand.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label "Name *", class:"form-label leftmargin" %>
    <%= form.text_field :brand_name, class:"form-control leftmargin edituserwidth" %>
  </div>

     <div class="field">
    <%= form.label :category, class:"form-label leftmargin" %><br/>
      <%= form.select :category_id, options_for_select(Category.all.current.collect { |p| [ p.name, p.id ] }, :selected=>brand.category_id), 
  {}, { class: "form-select leftmargin edituserwidth" } %>  
  </div>
<br/>
  <div class="actions">
    <%= form.submit class:"btn btn-secondary leftmargin" %>
  </div>

<% end %>
